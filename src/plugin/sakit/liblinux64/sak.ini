# Config file for linux64
BINESPEAK=speak_x64                 # speak or espeak binary name.
LIBESPEAK=                          # espeak library name (needed for espeak binary).
DIRESPEAKDATA=../                   # directory of espeak-data ( ./ or / => calling dir, ../ => parent dir).
LIBPORTAUDIO=libportaudio_x64.so    # portaudio library name.

